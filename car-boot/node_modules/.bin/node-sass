#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gulp-sass/node_modules/node-sass/bin/node-sass" "$@"
  ret=$?
else 
  node  "$basedir/../gulp-sass/node_modules/node-sass/bin/node-sass" "$@"
  ret=$?
fi
exit $ret
